let sumRange = (start: long, end: long) -> longer {
    let total = 0;

    for num in start..end {
        total = total + num;
    }

    return total;
}

const a = 1, b = 100;
const result: longer = sumRange(a, b);

print(result); // 5050
